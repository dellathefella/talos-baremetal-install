sudo cp talos-machineconfigs/*.yaml /var/lib/matchbox/assets &&

# Copying profiles configs 
sudo cp profiles/* /var/lib/matchbox/profiles &&

# Copying group configs
sudo cp groups/* /var/lib/matchbox/groups